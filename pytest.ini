[pytest]
testpaths = tests
norecursedirs =
    .venv
    cdk.out
    asset-output
    build
    dist
    node_modules
    __pycache__
addopts = -ra
markers =
    e2e: tests that exercise a deployed AWS stack and live external services
